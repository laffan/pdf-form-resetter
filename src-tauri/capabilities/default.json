{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the window is allowed to do: pick a file, and read the events it needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
